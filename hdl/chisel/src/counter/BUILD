load("//rules:rules.bzl", "gen_rtl_target")

gen_rtl_target(
    target_name="counter",
    emit_class = "counter.EmitCounter",
    srcs=["counter.scala"],
    deps=[],
    app_opts=["--dataBits=3"],
)
